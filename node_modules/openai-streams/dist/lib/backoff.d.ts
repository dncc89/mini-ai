export interface BackoffOptions {
    maxRetries: number;
    delay: number;
}
export declare const fetchWithBackoff: (input: RequestInfo, init?: RequestInit, { delay, maxRetries }?: BackoffOptions) => Promise<Response>;
